interface:
  display_name: "LDD Setup"
  short_description: "Bootstrap LDD local ledger"
  default_prompt: "Use $ldd-setup to run /ldd:setup for this repository."

policy:
  allow_implicit_invocation: true
